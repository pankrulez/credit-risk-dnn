# ⚠️ torch is NOT listed here — installed separately in Dockerfile
# with CPU-only wheel to save ~1.4GB of memory

scikit-learn>=1.3.0
shap
fairlearn
streamlit>=1.32.0
requests
pandas
numpy
matplotlib
python-dotenv
pytorch-tabnet
huggingface_hub>=0.20.0
fastapi
uvicorn[standard]
pydantic>=2.0
gunicorn